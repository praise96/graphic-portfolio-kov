# graphic-portfolio-kov

Quick notes:

- To preview locally run:

```powershell
cd C:\Users\praise\Desktop\kovykov\graphic-portfolio-kov
python -m http.server 8000
```

Then open http://localhost:8000 in your browser.

- If images don't appear when opening `index.html` directly with `file://`, prefer serving via HTTP (see above).
